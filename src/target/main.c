# include "ft_malcolm.h"

/* TODO: 
	- To keep it simple this program will just send an arp request to
		someone to be poisoned by malcolm
	- This program will create the socket ? So malcolm will need to connect 
		to it so it can receive the arp request ?
*/

int main(int argc, char **argv) {
	
}